def pomConfig = {
    licenses {
        license {
            name "The Apache Software License, Version 2.0"
            url "http://www.apache.org/licenses/LICENSE-2.0.txt"
            distribution "repo"
        }
    }
    developers {
        developer {
            name 'Mouhammed El Zaart'
        }
    }

    scm {
        url 'https://github.com/mzaart/View'
    }
}

def withPom = { pom ->
    pom.withXml {
        def root = asNode()
        root.appendNode('name', 'View')
        root.appendNode('description', 'Cross-platform UI library')
        root.appendNode('url', 'https://github.com/mzaart/View')
        root.children().last() + pomConfig
    }
}

ext.withPom = withPom